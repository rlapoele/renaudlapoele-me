---
import "@styles/global.css";

import type { LocaleType } from "@content/i18n/locale";
import type { ResumeType } from "@content/i18n/resume/Types";

import { getResumeData } from "@content/i18n/resume";
import { config } from "@content/config";
import { SITE_URL } from "astro:env/server";
import { normalizeSiteUrl } from "@scripts/server/configUtils.ts";

import Header from "@components/layout/Header/Header.astro";
import Footer from "@components/layout/Footer.astro";
import Icon from "@components/ui/Icon.astro";

interface Props {
  locale: LocaleType;
}

const { locale } = Astro.props;
const resume: ResumeType = getResumeData(locale);
const siteUrl = normalizeSiteUrl(SITE_URL);
---

<!DOCTYPE html>
<html lang={locale} class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{resume.content.hero.title}</title>
    <meta
      name="description"
      content="Resume landing page for Renaud Lapoele, Senior Front-End Engineer with UX/UI and product leadership experience."
    />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <style>
      @font-face {
        font-family: "Urbanist";
        src: url("/fonts/Urbanist/Urbanist-VariableFont_wght.ttf") format("truetype-variations");
        font-weight: 100 900;
        font-style: normal;
        font-display: swap;
        unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+2000-206F, U+2074, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
      }
      @font-face {
        font-family: "Urbanist";
        src: url("/fonts/Urbanist/Urbanist-Italic-VariableFont_wght.ttf") format("truetype-variations");
        font-weight: 100 900;
        font-style: italic;
        font-display: swap;
        unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+2000-206F, U+2074, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
      }
      @font-face {
        font-family: "Lora";
        src: url("/fonts/Lora/Lora-VariableFont_wght.ttf") format("truetype-variations");
        font-weight: 100 900;
        font-style: normal;
        font-display: swap;
        unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+2000-206F, U+2074, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
      }
      @font-face {
        font-family: "Lora";
        src: url("/fonts/Lora/Lora-Italic-VariableFont_wght.ttf") format("truetype-variations");
        font-weight: 100 900;
        font-style: italic;
        font-display: swap;
        unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+2000-206F, U+2074, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
      }
    </style>
  </head>

  <body>
    <div class="relative min-h-full">
      <div class="c-grid-overlay"></div>
      <div class="relative w-full">
        <a href="#main-content" class="c-skip-link">Skip to main content</a>
        <Header locale={locale} siteUrl={siteUrl} navItemsData={config.header.navigation.items} navItemLabels={resume.ui.header.navigation} settings={resume.ui.header.settings} />
        <main id="main-content" tabindex="-1" role="main">
          <slot />
        </main>
        <Footer class=""
                fullname={`${resume.data.personal.firstName} ${resume.data.personal.lastName}`}
                misc={resume.ui.misc}
                tagline={resume.content.footer.tagline}/>
      </div>
    </div>

    <script>
      import { setupInteractions } from "@scripts/client/interactions.ts";
      setupInteractions();
    </script>
  </body>
</html>
