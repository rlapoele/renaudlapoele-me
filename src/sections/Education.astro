---
import type {EducationDegreeType} from "@content/i18n/resume/Types.ts";
import Card from "@components/ui/Card.astro";
import Section from "@components/layout/Section.astro";
import Bullet from "@components/ui/Bullet.astro";

type Props = { content: EducationDegreeType, sectionLabel: string };
const { content, sectionLabel } = Astro.props as Props;
---
<Section id="education" label={sectionLabel} title={content.title} bodyClass="space-y-5">
  <Card>
  <!--
    <ul class="space-y-2 text-sm text-ink-primary">
      {
        content.items.map((languageItem) => (
          <li class="flex items-center gap-3">
            <Bullet class="mbs-0.5"/><span class="font-medium">{languageItem.name} <span class="text-ink-tertiary">({languageItem.level})</span></span>
          </li>
        ))
      }
    </ul>
    -->
  </Card>

</Section>
