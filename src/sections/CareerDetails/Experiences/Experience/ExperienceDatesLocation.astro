---
import DatesLocation from "@sections/CareerDetails/shared/DatesLocation.astro";
type Props = {
  id: string;
  startDateLabel: string
  endDateLabel: string
  location: string
  class?: string
};

const {
  id,
  startDateLabel,
  endDateLabel,
  location,
  class: className = ""
} = Astro.props as Props;

---
<DatesLocation id={id} class={className} startDateLabel={startDateLabel} endDateLabel={endDateLabel} location={location} />
